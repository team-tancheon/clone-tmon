package com.tancheon.tmon.repository;

import com.tancheon.tmon.domain.Dom;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DomRepository extends JpaRepository<Dom, Long> {
//public interface DomRepository extends JpaRepository<Dom, String> {
}
